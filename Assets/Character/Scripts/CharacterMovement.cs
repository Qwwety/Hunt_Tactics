using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.Tilemaps;

public class CharacterMovement : MonoBehaviour
{

    private void Update()
    {
        //if (Input.GetMouseButtonDown(0))
        //{
        //    this.transform.position = Camera.main.ScreenToWorldPoint(GetMousePos());// IsometricToXY(GetMousePos().x, GetMousePos().y);
        //}
    }

}
